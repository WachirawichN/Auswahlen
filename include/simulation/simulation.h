#pragma once

#include <graphic/window.h>
#include <graphic/pipeline.h>

namespace auswahlen
{
    class simulation
    {
        private:
        public:
            simulation();
            ~simulation();
            void stepSimulation();
    };
}
